[project]
name = "xuexitong"
version = "0.2.0"
description = "超星学习通课程自动化助手 — OpenClaw skill + Chrome extension + bridge server"
requires-python = ">=3.13"
dependencies = [
  "websockets>=15.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
